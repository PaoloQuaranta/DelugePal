# Trip-hop — piano di implementazione

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** il trip-hop (terzo genere del perimetro 3): la fusione hip-hop + dub + armonia minore jazzy, con l'eco dub come primitiva nuova.

**Architecture:** una primitiva `MU.eco_dub` (delay analog/synced/feedback), un esempio `tools/triphop_scritto.py` che ricombina batteria hip-hop rallentata, basso dub e il vamp minore sul Rhodes (con eco), l'istruzione e la scheda nuova. L'armonia si chiude col `[CALC]`; il resto con l'ascolto.

**Tech Stack:** Python stdlib + libreria `delugexml`. Test in `tests/test_all.py`. Trasferimento via `tools/dsysex.py` da PowerShell.

## Global Constraints

- **Mai scrivere XML a mano** — solo la libreria; attributi solo se esistenti/verificati.
- **Fine-riga LF** su codice/istruzioni; `git diff --stat` == `--ignore-cr-at-eol` prima di ogni commit; **HANDOFF.md è CRLF** (memoria `fine-riga-nel-repo`).
- **`print()` solo ASCII** (console cp1252).
- **`MU.verifica(doc)` vuota** prima di dichiarare caricabile.
- **Metodo misto:** armonia = `[CALC]` (già fatto: `racconta_armonia` sul vamp, 18 set 2026); batteria/basso/eco = ascolto dell'utente. I **livelli** (feedback eco, swing, velocity) sono sfumature, non leggi (memoria `livelli-sfumature-non-leggi`): argomenti regolabili, non da tarare.
- **Contratto delle schede**: titoli caselle esatti + indice coerente; **suite INTERA** prima di committare la scheda.
- **Attribuzione commit:** `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.

Fatti grounded:
- `MU.armonia(spec, *, voicing, registro, durata='1/1', velocity, condotta=True) -> dict[int,list[Note]]` — il vamp a battute intere, condotto, pronto per `MU.scrivi`.
- Vamp `Cm9 | Cm9 | Abmaj7 | G7b9` verificato col `[CALC]`: Cm9 rootless E♭ G B♭ D, Abmaj7 C E♭ G B♭, G7♭9 A♭ B D F.
- `Tal Rhodes.XML`, `Square Saw Bass.XML`, `KIT009.XML` (drum `KICK`/`SNARE`/`HATC`) sono le fixture di `hiphop_scritto.py`. Tal Rhodes ha `delayRate`, `delayFeedback` e `<delay pingPong analog syncLevel syncType>` → `eco_dub` funziona.
- `add_track` sposta `<defaultParams>` sul `<params>` della clip: `delayFeedback` vive sulla clip, l'elemento `<delay>` sullo strumento.

---

## File Structure

- `tools/delugexml/musica.py` — **modifica**: `eco_dub` (nuova, dopo `acid`).
- `tests/test_all.py` — **modifica**: `test_eco_dub`, `test_triphop_scritto`.
- `tools/triphop_scritto.py` — **crea** (scheletro da `hiphop_scritto.py`).
- `docs/istruzioni/trip-hop.md` — **crea**.
- `docs/repertori/trip-hop.md` — **crea** (nuova scheda).
- `docs/MUSICA.md` — **modifica**: nuova riga d'indice per trip-hop.

---

## Task 1: `MU.eco_dub` — l'eco dub

**Files:**
- Modify: `tools/delugexml/musica.py` (nuova `eco_dub`, dopo `acid`)
- Test: `tests/test_all.py` (`test_eco_dub`)

**Interfaces:**
- Consumes: `sound.set`/`container`, `song.clips`/`instrument_of`, `parser.Node`.
- Produces: `eco_dub(doc, bersaglio, *, feedback=35, sync=7, analog=True, pingpong=True, rate=None) -> dict` — sull'strumento imposta `<delay>` (analog, pingPong, syncLevel/syncType); su ogni clip `delayFeedback` (e `delayRate` se `rate`). Ritorna un racconto con `clip` e `da_verificare=True`.

- [ ] **Step 1: Write the failing test**

```python
def test_eco_dub():
    """MU.eco_dub: delay analog+synced sullo strumento, feedback sulle clip."""
    from delugexml import musica as MU                          # noqa: PLC0415
    from delugexml import sound as SND, create as C            # noqa: PLC0415
    import warnings                                             # noqa: PLC0415
    preset = REFS / 'synths' / 'Tal Rhodes.XML'
    tem = REFS / 'songs' / 'TEMPL0.XML'
    if not (preset.exists() and tem.exists()):
        salta('test_eco_dub', 'manca una fixture')
        return
    with warnings.catch_warnings():
        warnings.simplefilter('ignore')
        doc = parse_file(tem)
        iR, cR = C.add_track(doc, str(preset), name='RHODES', folder='SYNTHS',
                             length=384, playing=True)
        r = MU.eco_dub(doc, iR, feedback=36, sync=7, analog=True, pingpong=True)
    d = iR.find('delay')
    check('il delay e analog (il degrado dub)', d is not None and d.get('analog') == '1',
          str(d and d.attrs))
    check('il delay e sincronizzato', d.get('syncLevel') == '7', d.get('syncLevel'))
    check('il ping-pong e acceso', d.get('pingPong') == '1', d.get('pingPong'))
    check('il feedback e scritto sulla clip', SND.get(cR, 'delayFeedback') == 36,
          str(SND.get(cR, 'delayFeedback')))
    check('eco_dub conta le clip e dichiara da_verificare',
          r.get('clip') == 1 and r.get('da_verificare') is True, str(r))
    check('il documento resta valido', MU.verifica(doc) == [], str(MU.verifica(doc)))
```

- [ ] **Step 2: Run test to verify it fails**

Run: `.venv/Scripts/python.exe -c "import sys; sys.path.insert(0,'tests'); import test_all; test_all.test_eco_dub()"`
Expected: FAIL — `MU.eco_dub` non esiste.

- [ ] **Step 3: Write minimal implementation** (in `musica.py`, dopo `acid`)

```python
def eco_dub(doc, bersaglio, *, feedback: int = 35, sync: int = 7,
            analog: bool = True, pingpong: bool = True, rate=None) -> dict:
    """Accende l'ECO DUB sul synth `bersaglio` (nodo strumento) -- l'eco a tempo
    che degrada, il respiro del dub e del trip-hop. Riusabile per dub e reggae.

    Due livelli, come `MU.acid`/`MU.sidechain`:
    - STRUMENTO (elemento <delay>): `analog` = il degrado caldo a ogni ripetizione
      (il suono del dub); `pingPong` per l'ampiezza; `syncLevel=sync`/`syncType=0`
      = l'eco sincronizzata al tempo;
    - OGNI CLIP del bersaglio: `delayFeedback` (le ripetizioni, unita' display
      0-50); se `rate` e' dato, anche `delayRate` (eco libera, non sincronizzata).

    ⚠️ I livelli (feedback, sync) sono `[DEC]`+`[da verificare]`: si giudicano
    all'orecchio. La struttura e' `[OSS]` (attributi nel preset).
    """
    from . import sound as SND                                  # noqa: PLC0415
    from . import song as S                                     # noqa: PLC0415
    from .parser import Node                                    # noqa: PLC0415

    d = bersaglio.find('delay')
    if d is None:
        d = bersaglio.append(Node(tag='delay'))
    d.set('analog', '1' if analog else '0')
    d.set('pingPong', '1' if pingpong else '0')
    d.set('syncLevel', str(sync))
    d.set('syncType', '0')

    n = 0
    for _, clip in S.clips(doc):
        if S.instrument_of(doc, clip) is bersaglio:
            SND.set(clip, 'delayFeedback', feedback)
            if rate is not None:
                SND.set(clip, 'delayRate', rate)
            n += 1
    return {'feedback': feedback, 'sync': sync, 'analog': analog,
            'pingpong': pingpong, 'clip': n, 'da_verificare': True}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `.venv/Scripts/python.exe -c "import sys; sys.path.insert(0,'tests'); import test_all; test_all.test_eco_dub()"`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add tools/delugexml/musica.py tests/test_all.py
git commit -m "feat: MU.eco_dub -- l'eco dub (delay analog/synced/feedback), riusabile

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 2: `tools/triphop_scritto.py` — il pezzo Portishead-ish

**Files:**
- Create: `tools/triphop_scritto.py`
- Test: `tests/test_all.py` (`test_triphop_scritto`)

**Interfaces:**
- Consumes: `MU.armonia`, `MU.eco_dub`, `MU.forma`, `MU.passi`, `MU.applica_verbo`, `create.add_track`, `song.duplicate_clip`, `arranger`, `sound`.
- Produces: `costruisci() -> tuple[doc, dict]`; `strumento(doc, nome)`; costanti.

Scheletro da `hiphop_scritto.py`. Differenze:
- `PRESET_RHODES = Tal Rhodes.XML`, `PRESET_BASSO = Square Saw Bass.XML`, `KIT = KIT009.XML`.
- `BPM = 84`, `SWING = 56` (lilt lo-fi laid-back, **non** dritto).
- **scala Do minore**: `S.set_scale(doc, 'C', 'minore')` (verificato: `'minore'` è un modo valido).
- `VAMP = ['Cm9', 'Cm9', 'Abmaj7', 'G7b9']` (4 battute); `SIGLE = VAMP` (il giro).
- **armonia (Rhodes)**: `comping()` usa `MU.armonia`:

```python
def comping() -> dict:
    """Il vamp minore jazzy, tenuto e molle sul Rhodes -- condotto (voice-leading).
    L'armonia e' chiusa col [CALC]; qui la si posa."""
    return MU.armonia(' | '.join(VAMP), voicing='senza-fondamentale',
                      registro='do3', durata='1/1', velocity=55)
```

- **batteria** `triphop_batteria()`: boom-bap **rallentato e morbido** — cassa 1-3 + una sincope (come hiphop `KICK_A`/`KICK_B`), backbeat sul 2 e 4 **più morbido** (velocity ~106, non 127), ghost molli (~40), hi-hat in **crome** con aria. Swing 56 lo fa laid-back. Drum names `KICK`/`SNARE`/`HATC`:

```python
def triphop_batteria() -> dict:
    from delugexml.notes import Note                          # noqa: PLC0415
    voci = {'KICK': [], 'SNARE': [], 'HATC': []}
    for bar in range(len(VAMP)):
        da = bar * B
        voci['KICK'] += MU.passi('x.x.....x.......' if bar % 2 == 0
                                 else 'x.......x...x...', da=da, velocity=64)
        for i in range(8):                                   # hi-hat crome, molli, con aria
            pos = da + i * CROMA
            voci['HATC'].append(Note(pos=pos, length=P, velocity=44 if (pos % MOV) else 36))
        for b in (MOV, 3 * MOV):                             # backbeat 2 e 4, MORBIDO (non 127)
            voci['SNARE'].append(Note(pos=da + b, length=P, velocity=106))
        for g in ([7, 9] if bar % 2 == 0 else [9, 15]):     # ghost molli
            voci['SNARE'].append(Note(pos=da + g * P, length=P, velocity=40))
    return voci
```

- **basso** `triphop_basso()`: dub sub, **rado e tenuto**, segue le fondamentali del vamp (C, C, A♭, G), grave, dietro il beat:

```python
def triphop_basso() -> dict:
    from delugexml.notes import Note                          # noqa: PLC0415
    voce: dict = {}
    rif = 33
    for bar, sig in enumerate(VAMP):
        da = bar * B
        root = _grave(MU.sigla(sig).fondamentale, rif); rif = root
        voce.setdefault(root, []).append(Note(pos=da, length=MIN + 96, velocity=92))   # 1: sub lungo
        voce.setdefault(root, []).append(Note(pos=da + 2 * MOV, length=MOV, velocity=80))  # 3: ribattuto
    return voce
```

(`_grave` e le costanti `B/MOV/MIN/CROMA/P` come in `hiphop_scritto.py`.)

- **il suono e lo spazio**: dopo aver scritto le clip del Rhodes, `MU.eco_dub(doc, iR, feedback=36, sync=7, analog=True, pingpong=True)` e un velo di riverbero `SND.set(cR, 'reverbAmount', 22)`; il basso `MU.applica_verbo(doc, iB, 'piu scuro')`.
- **la forma** con `MU.forma`: intro (solo Rhodes, 4 battute) → full (tutti, 8 battute). Rhodes in entrambe (stessa clip); batteria e basso solo in full. Clip di sezione con `song.duplicate_clip` **non servono** qui: ogni strumento ha una clip sola, riusata (il Rhodes) o messa in una sola sezione (batteria/basso). Quindi:

```python
    sezioni = {'intro': [cR], 'full': [cR, cK, cB]}
    MU.forma(doc, 'intro full', sezioni, battute_per={'intro': 4, 'full': 8})
```

⚠️ `MU.forma` mette la STESSA clip `cR` in intro e full: nel full si ripete in loop (clip 4 battute, sezione 8). Batteria e basso (clip 4 battute) nel full loopano ×2.

- [ ] **Step 1: Write the failing test**

```python
def test_triphop_scritto():
    """triphop_scritto.py: la fusione -- vamp minore sul Rhodes con eco dub,
    batteria hip-hop rallentata, basso dub."""
    from delugexml import musica as MU                          # noqa: PLC0415
    from delugexml import sound as SND, arranger as AR         # noqa: PLC0415
    try:
        import triphop_scritto as TH                            # noqa: PLC0415
    except FileNotFoundError:
        salta('test_triphop_scritto', 'manca una fixture')
        return
    try:
        doc, _ = TH.costruisci()
    except FileNotFoundError:
        salta('test_triphop_scritto (build)', 'manca una fixture')
        return
    check('il pezzo TRIP-HOP e valido', MU.verifica(doc) == [], str(MU.verifica(doc)))
    check('nessuna avvertenza', MU.avvertenze(doc) == [], str(MU.avvertenze(doc)))
    est = AR.extent(doc)
    check('l arco e lungo 12 battute',
          est is not None and est[1] == 12 * MU.TICK_PER_BATTUTA, str(est))
    iR = TH.strumento(doc, 'RHODES')
    d = iR.find('delay')
    check('il Rhodes ha l eco dub (analog)', d is not None and d.get('analog') == '1',
          str(d and d.attrs))
    rho = [c for _, c in S.clips(doc) if S.instrument_of(doc, c) is iR][0]
    check('il feedback dell eco e sulla clip del Rhodes',
          SND.get(rho, 'delayFeedback') is not None)
    # l'armonia: le classi di altezza del vamp (Cm9 rootless: Eb G Bb D)
    import warnings
    pcs = {n.pitch % 12 for note in TH.comping().values() for n in note}
    check('l armonia contiene le note del vamp (Eb,G,Bb,D di Cm9)',
          {3, 7, 10, 2} <= pcs, str(sorted(pcs)))
```

- [ ] **Step 2: Run to fail** — `import triphop_scritto` FileNotFound.
- [ ] **Step 3: Write `tools/triphop_scritto.py`** (scheletro `hiphop_scritto.py` + le funzioni sopra + `costruisci()` che carica TEMPL0, set bpm/swing/scala, `MU.togli`, add_track RHODES/SUB/KIT009, scrive comping/basso/batteria, `eco_dub`+reverb, `MU.forma`, `open_in_arranger`; `__main__` stampa extent/verifica/avvertenze).
- [ ] **Step 4: Run module then test** — `.venv/Scripts/python.exe tools/triphop_scritto.py` (verifica vuota), poi il test. Expected: PASS.
- [ ] **Step 5: Commit**

```bash
git add tools/triphop_scritto.py tests/test_all.py
git commit -m "feat: triphop_scritto -- la fusione (vamp minore + eco dub, batteria hip-hop rallentata, basso dub)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3: l'istruzione `docs/istruzioni/trip-hop.md`

**Files:** Create: `docs/istruzioni/trip-hop.md`

Una sola (fusione). Contenuto: cos'è (downtempo scuro cinematico; hip-hop+dub+jazz); il feel (lento ~70-90, laid-back, swing leggero); la batteria (boom-bap rallentato, più morbido e con aria → rimanda a `batteria-hiphop.md` variante lo-fi, non ripetere); il basso (dub, sub, rado → rimanda a `basso-hiphop.md`); l'armonia (il cuore: minore jazzy, prestiti ♭VI, dominanti alterate, Rhodes rootless → moduli d'armonia; *il sapore è scelta compositiva*); lo spazio (`MU.eco_dub` + riverbero); i gradi (`[LIB]`+`[DEC]`; batteria appoggiata al `[MIS]` boom-bap; eco `[OSS]`+`[da verificare]`; armonia `[CALC]`); l'esempio `tools/triphop_scritto.py`; cosa manca (vocal breathy, vinyl crackle — `audio.py`). Verdetto d'ascolto: **in sospeso**.

- [ ] Step 1: scrivere (LF). Step 2: fine-riga OK. Step 3: commit `istruzioni: trip-hop ...`.

---

## Task 4: la scheda `docs/repertori/trip-hop.md` e l'indice

**Files:** Create `docs/repertori/trip-hop.md`; Modify `docs/MUSICA.md`.

- [ ] **Step 1: scrivere la scheda** — 11 caselle, **titoli identici** alle altre schede (contratto). Piene: 1 (cos'è: fusione downtempo), 3 (tempo ~70-90, esempio 84), 4 (feel laid-back, swing 56), 5 (ruoli: batteria polverosa, basso dub, Rhodes armonia, spazio), 6 (dinamica morbida), 7 (armonia minore jazzy `[CALC]`, il vamp), 10 (sul Deluge: `eco_dub`, Tal Rhodes, KIT009, Square Saw Bass). Parziali/vuote **dichiarate**: 2 (metro: 4/4 16 passi), 8 (topline: vocal/sample → `audio.py`), 9 (forma: intro→full, accennata), 11 (trappole del generatore). Fonti: rimandi a hip-hop/dub/armonia, l'esempio, il verdetto in sospeso.
- [ ] **Step 2: aggiornare `docs/MUSICA.md`** — scorporare **trip hop** dalla riga aggregata `elettronica · IDM · trip hop · DnB · jungle` in una **riga propria col link** (come hip hop e house), con i simboli `●`/`◐`/`○` per casella; aggiornare la riga aggregata togliendo «trip hop»; aggiornare la nota in coda («Hip hop e house/techno hanno una scheda…») per includere trip-hop.
- [ ] **Step 3: fine-riga** sui due file.
- [ ] **Step 4: suite INTERA** — `.venv/Scripts/python.exe tests/test_all.py`, verde.
- [ ] **Step 5: commit** `repertori: trip-hop -- scheda nuova e riga nell'indice`.

---

## Task 5: caricamento e ascolto

- [ ] **Step 1:** generare con `triphop_scritto.costruisci()` + `write_file`; `verifica()` vuota.
- [ ] **Step 2:** `MU.destinazione('triphop', 1, 'SONGS')` → `/SONGS/DelugePal/TRIPHOP01.XML` (verificato; se esiste, incrementare la versione).
- [ ] **Step 3:** trasferire con `tools/dsysex.py` **da PowerShell**, senza troncare l'output di `put`. Se il Deluge non risponde al `ping`: scrivere il file locale e **dirlo**.
- [ ] **Step 4: l'utente ascolta.** L'armonia è già chiusa col `[CALC]`; all'orecchio: il vamp minore ha il colore giusto (♭VI caldo, G7♭9 scuro)? l'eco dub respira (analog, feedback)? la batteria è laid-back e polverosa? il sub dub regge?
- [ ] **Step 5: pinnare i `[da verificare]`.** Col verdetto: fissare i livelli (feedback eco, swing, velocity) se serve, scrivere il verdetto nella scheda/istruzione, aggiornare il HANDOFF (CRLF). Commit finale.

---

## Self-Review

**1. Spec coverage:** `MU.eco_dub` → Task 1; vamp minore col `[CALC]` → Task 2 (comping via `MU.armonia`); batteria hip-hop rallentata/morbida → Task 2; basso dub → Task 2; forma intro→full → Task 2; istruzione → Task 3; scheda + riga d'indice nuova → Task 4; ascolto → Task 5; sapore Portishead (scuro, armonia al centro) → il vamp + Rhodes + eco. YAGNI (niente vocal/crackle, niente primitiva laid-back, niente collage) rispettato.

**2. Placeholder scan:** nessun TODO. Le funzioni-nota di Task 2 hanno codice concreto; `costruisci()` segue `hiphop_scritto.py` (template committato). L'unico ramo condizionale è la scala Do minore (`'minore'` o relativa `Eb maggiore`), da verificare sul volo — non un buco di contenuto.

**3. Type consistency:** `eco_dub`/`comping`/`triphop_batteria`/`triphop_basso`/`costruisci`/`strumento(doc,nome)` con le stesse firme in Task 1-2 e nei test. `delayFeedback`/`<delay>`/`analog`/`syncLevel` coerenti col preset. Vamp `Cm9|Cm9|Abmaj7|G7b9` identico ovunque, `[CALC]`-verificato.
